import * as React from "react"
import Template from "../templates/template"

// markup
const NotFoundPage = () => {
  return (
    <Template>
      <h1>Not found..</h1>
    </Template>
  )
}

export default NotFoundPage
